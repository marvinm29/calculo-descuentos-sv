# Clerk (obligatorio — reemplazar con tu secret key)
CLERK_SECRET_KEY=YOUR_CLERK_SECRET_KEY_HERE

# Base de datos (opcional — default: ./data/calculos.db)
DATABASE_PATH=./data/calculos.db

# Puerto (opcional — default: 3001)
PORT=3001

# Entorno (opcional — default: development)
NODE_ENV=production

# Sentry (opcional)
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# Datadog (opcional — se configura vía env vars, no necesita código)
# DD_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# DD_SITE=datadoghq.com
